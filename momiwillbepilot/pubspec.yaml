name: momiwillbepilot
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  crypto: ^3.0.6
  community_charts_flutter: ^1.0.2
  file_picker: ^10.3.3
  share_plus: ^12.0.0
  universal_html: ^2.2.4
  fl_chart: ^1.1.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/unique_questions.json
